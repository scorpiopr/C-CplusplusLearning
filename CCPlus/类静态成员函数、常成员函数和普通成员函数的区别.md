# 一、 核心区别全景对比表

| 对比维度 | 普通成员函数 | 静态成员函数 | const 成员函数 |
|---|---|---|---|
| 归属权 | 属于具体的对象实例（Object） | 属于整个类（Class） | 属于具体的对象实例（Object） |
| 隐式参数 | 包含 this 指针（类型为 T* const） | 无 this 指针 | 包含常 this 指针（类型为 const T* const） |
| 成员访问权限 | 可直接读写类内所有静态与非静态成员 | 只能直接访问静态成员，无法访问非静态成员 | 只能读取非静态成员，可读写静态成员与 mutable 成员 |
| 虚函数特性 | 可以是虚函数（virtual），支持多态 | 绝对不能是虚函数，不支持动态绑定 | 可以是虚函数（virtual），支持多态 |
| const 修饰 | 尾部不能用 const 修饰 | 绝对不能用 const 修饰 | 尾部必须用 const 修饰（如 void func() const） |
| 调用前置条件 | 必须先实例化对象才能调用 | 无需实例化对象，通过类名即可随时调用 | 必须先实例化对象才能调用 |
| 对象接收限制 | 常对象（const object）无法调用普通成员函数 | 常对象和普通对象都可以正常调用 | 常对象与普通对象都可以调用（常对象只能调用它） |

------------------------------
# 二、 底层内存与编译原理（面试加分杀手锏）
假设有一个类 Player，内含一个普通函数 move 和一个静态函数 getCount：

```cpp
class Player {
public:
    int m_score{0};
    static int s_count;
    mutable int m_cachedData{0}; // 引入 mutable 关键字用于演示

    void move(int x);              // 1. 普通成员函数
    static int getCount();         // 2. 静态成员函数
    void showScore() const;        // 3. const 成员函数（常成员函数）
};
```
在编译期，编译器在底层会把这些函数强制“脱掉面向对象的外衣”，翻译成类似 C 语言的全局函数：
## 1. 普通成员函数的底层翻译

```cpp
// 编译器偷偷塞进去一个隐式的第一形参：Player* const this
void Player_move(Player* const this, int x) {
    this->m_score += x; // 🔑 所有对非静态成员的访问，底层都是通过 this 指针完成的
}
```

* 调用本质：当你在代码中写 ```p.move(10);``` 时，底层机器码其实是在执行 ```Player_move(&p, 10);```。

## 2. 静态成员函数的底层翻译

```cpp
// 翻译后就是一个纯粹的全局函数，没有任何隐式参数！
int Player_getCount() {
    return s_count; // ✅ 只能访问同样属于类的静态变量
    // return m_score; // ❌ 报错：因为没传 this 指针进来，根本不知道去哪读 m_score
}
```

## 3. const 成员函数的底层翻译

```cpp
// 🔑 编译器塞进去一个指向常量的隐式第一形参：const Player* const this
void Player_showScore(const Player* const this) {
    int current = this->m_score; // ✅ 纯读取操作，合法
    
    // this->m_score += 10; 
    // ❌ 报错：this 指向的对象是 const，里面的非静态成员变成只读，拒绝修改！
    
    this->m_cachedData = 100;    
    // ✅ 合法：被 mutable 修饰的变量，在底层不受 const 指针的限制
}
```

* 调用本质：当你在代码中写 ```p.showScore();``` 时，底层机器码其实是在执行 ```Player_showScore(&p);```。
* 常对象契合：如果你定义了一个常对象 ```const Player cp;```，它的地址类型是 ```const Player*```。由于普通成员函数接收 Player*（无法隐式转换为指针的安全性降级），所以常对象只能成功调用接收 const Player* 的 const 成员函数或无隐式参数的静态成员函数。

------------------------------
# 三、 注意点
## 1. 为何静态成员函数不能是虚函数（virtual）？
面试官高频追问：“既然普通函数可以是虚函数，为什么静态函数加上 virtual 编译会直接报错？”

* 答案：我们在多态与虚表机制中复习过，虚函数的动态绑定必须经历“三步走”：首先必须通过对象头部的 this 指针找到 vptr（虚表指针），进而查 vtable 才能跳转。而静态成员函数底层压根没有 this 指针，它甚至可以在没有对象的时候被调用，这意味着它根本无法触及 vptr。没有了查表的硬件基础，动态多态自然无从谈起。

## 2. 常成员函数核心考点

### this 指针的本质变化：
* 普通成员函数中，this 的类型是 T* const（指针本身是常量，指向的内容可变）。
* const 成员函数中，this 的类型变成了 const T* const（指针本身和指向的对象都是常量），因此任何试图修改成员变量的代码都会引发编译错误。
### 如何突破 const 限制（mutable 关键字）：
* 如果某个成员变量被 mutable 修饰，那么即使在 const 成员函数中，也可以对它进行修改（常用于缓存锁、引用计数或调试日志）。
### 函数重载（Overload）：
* 仅靠尾部有无 const 可以构成合法的函数重载。
* 当对象是常对象时，

## 3. 工业级典型应用场景
在大型工程（如线程池、设计模式、网络库）中，我们是如何做选择的？
### 普通成员函数的场景

* 定位：用于描述对象的具体行为，需要读取或修改对象自身的特有属性（如角色的生命值减扣、文件的写入操作）。

### 静态成员函数的场景

* 工具类函数：如数学库 Math::sin(double x)，计算不依赖任何对象状态，仅依赖入参。
* 单例模式（Singleton）：用于获取全局唯一的静态实例指针（如我们前面复习过的迈耶斯单例 getInstance()）。
* 线程/异步回调函数：在调用操作系统底层的线程库（如 Linux 的 pthread_create）时，传入的回调函数必须是 C 风格的全局函数指针。此时，普通成员函数因为带有隐式的 this 指针导致指针类型不匹配而无法传入，必须将回调函数声明为静态成员函数才能完美契合。

### const 成员函数的场景

* 对象的只读查询接口：任何只读取、不修改对象状态的函数（如 size()、empty()、getScore()）。在工业级代码中，这类函数必须显式声明为 const。
* 标准库容器与算法的兼容（以 std::map/std::set 为例）：当自定义结构体作为 std::set 的元素或 std::map 的 Key 时，容器内部需要调用重载的比较运算符（如 operator<）。因为容器内部存储的 Key 是只读的，所以自定义的比较函数必须是 const 成员函数，否则会导致编译报错。
* 多线程并发下的“常安全（Const Safety）”：在多线程开发中，只读操作是天然线程安全的。如果一个对象的方法被声明为 const，其他开发者或代码审查者就能立刻明白：该方法可以在多个线程中被并发调用，而无需加锁（只要没有使用 mutable 破坏内部常属性）。
* 延迟加载与性能缓存（结合 mutable）：在图形学、网络库中，某些数据的计算非常耗时（如计算复杂的 3D 矩阵、解析大段 JSON 配置）。我们通常在类中提供一个 const 的获取接口，在接口内部利用 mutable 修饰的缓存变量。当第一次调用时进行计算并写入缓存，后续调用直接读取缓存，对外既保证了对象的“逻辑只读”，又优化了执行性能。

